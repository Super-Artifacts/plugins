# Generating the connect pages

`node scripts/generate-connect-docs.mjs` rewrites every page in this directory
except this README from `AGENT_GUIDES` (`apps/platform/src/lib/agent-guides.ts`).

Run it whenever the guides change; the diff shows exactly what a harness's
instructions now say.